Debug the login failure in the AEXON App.

Current problem:
The login screen shows "Request failed (404)" when trying to authenticate.

Goal:
Ensure the AEXON App correctly connects to the AEXON Connect API.

Steps to perform:

1. Check environment variable
VITE_AEXON_CONNECT_API_URL

Ensure all API requests use this variable as the base URL.

Example:

const API_BASE = import.meta.env.VITE_AEXON_CONNECT_API_URL;

2. Inspect the login request in Launcher.tsx.

Ensure login calls:

POST /auth/login

Example request:

fetch(`${API_BASE}/auth/login`, {
  method: "POST",
  headers: {
    "Content-Type": "application/json"
  },
  body: JSON.stringify({
    email,
    password
  })
})

3. Verify there are no remaining Supabase auth calls.

The app must NOT use Supabase authentication anymore.

4. Add debugging logs.

Before sending the login request print:

console.log("API URL:", API_BASE);
console.log("Login endpoint:", `${API_BASE}/auth/login`);

5. Add proper error handling.

If the response status is not OK, show the returned error message instead of generic "404".

6. Verify all API requests in the app use the helper file:

src/lib/aexonConnect.ts

Create a reusable API client if it doesn't exist.

Example:

export async function apiRequest(path, options = {}) {
  const base = import.meta.env.VITE_AEXON_CONNECT_API_URL;

  const response = await fetch(`${base}${path}`, {
    ...options,
    headers: {
      "Content-Type": "application/json",
      ...(options.headers || {})
    }
  });

  return response.json();
}

7. Update login to use this helper.

Example:

await apiRequest("/auth/login", {
  method: "POST",
  body: JSON.stringify({ email, password })
});

8. Verify that the correct endpoint path is used.

The correct login endpoint is:

POST /auth/login

NOT:

/login
/api/login
/authenticate

9. After login success:

Store JWT token in localStorage:

localStorage.setItem("aexon_token", token);

10. Ensure all protected requests send Authorization header:

Authorization: Bearer <token>

Return a summary of fixes and confirm that login now correctly reaches the AEXON Connect API.