You are improving the Aexon Endoscopy App. Read ALL existing files carefully. 
Apply these UI and sidebar improvements:

═══════════════════════════════════════
1. SIDEBAR — VISUAL OVERHAUL (MainLayout.tsx)
═══════════════════════════════════════

REMOVE: "Paket & Berlangganan" menu item entirely from nav items.
Replace with a subscription status chip in the bottom section only.

SIDEBAR STRUCTURE (top to bottom):
┌─────────────────────────────┐
│ [Logo] Aexon    [collapse]  │  ← header, border-b
├─────────────────────────────┤
│  [icon] Beranda             │  ← nav items
│  [icon] Mulai Sesi Baru     │
├─────────────────────────────┤
│                             │  ← flex-1 spacer
├─────────────────────────────┤
│ ┌─────────────────────────┐ │  ← subscription status card
│ │ ● Aktif                 │ │
│ └─────────────────────────┘ │
│  [icon] Pengaturan          │
│  [avatar] Nama — badge      │
│  [icon] Keluar              │
│  ● online/offline dot       │
└─────────────────────────────┘

SUBSCRIPTION STATUS CHIP:
State 1 — AKTIF (plan === 'subscription' || plan === 'enterprise'):
- Green dot + "Aktif"
- bg-emerald-50, border border-emerald-200, rounded-xl
- text-emerald-700 font-semibold text-xs
- NO CTA button

State 2 — WARNING H-7 (trialDaysLeft !== null && trialDaysLeft <= 7 && trialDaysLeft > 0):
- Orange dot animate-pulse + "Trial — X hari lagi"
- If expanded: "Segera berlangganan" CTA → onNavigate('settings')
- bg-orange-50, border border-orange-200, rounded-xl
- text-orange-700 font-semibold text-xs

State 3 — TRIAL NORMAL (trialDaysLeft > 7):
- Yellow dot + "Trial — X hari lagi"
- bg-yellow-50, border border-yellow-100, rounded-xl
- text-yellow-700 text-xs
- NO CTA

State 4 — TIDAK AKTIF (plan === null && !trialDaysLeft):
- Red dot animate-pulse + "Tidak Aktif"
- If expanded: "Berlangganan Sekarang" CTA → onNavigate('settings')
- bg-red-50, border border-red-200, rounded-xl
- text-red-700 font-semibold text-xs

COLLAPSED SIDEBAR:
- Show only colored dot, no text
- Tooltip on hover showing full status
- Dot: w-2.5 h-2.5, animate-pulse for warning/inactive

ADMIN INSTITUSI: always show green "Aktif" chip, no CTA
DOKTER INSTITUSI: show based on enterprise plan status

═══════════════════════════════════════
2. SIDEBAR NAV ITEMS — CLEANUP
═══════════════════════════════════════
Doctor (personal + institusi) nav items:
- Beranda (LayoutDashboard icon)
- Mulai Sesi Baru (Plus icon)

Admin nav items:
- Admin Console (LayoutDashboard icon)
- Kop Surat Institusi (FileText icon)

Bottom (all accounts):
- Subscription status chip
- Pengaturan (Settings icon)
- User info (avatar initials + name + role badge)
- Keluar (LogOut icon, red)
- Online/offline dot indicator

═══════════════════════════════════════
3. GLOBAL UI — ALL PAGES
═══════════════════════════════════════

TYPOGRAPHY:
- Page titles: text-2xl font-bold text-slate-900 tracking-tight
- Section headers: text-sm font-semibold text-slate-500 uppercase tracking-wider mb-4
- Body: text-sm text-slate-600
- Labels: text-xs font-medium text-slate-500

CARDS:
- All cards: bg-white rounded-2xl border border-slate-100 shadow-sm p-6
- Hover cards: hover:shadow-md hover:border-slate-200 transition-all duration-200

BUTTONS:
- Primary: bg-[#0C1E35] text-white hover:bg-[#1a3a5c] rounded-xl px-4 py-2.5 text-sm font-semibold
- Secondary: border border-slate-200 text-slate-700 hover:bg-slate-50 rounded-xl px-4 py-2.5 text-sm font-semibold
- Danger: bg-red-600 text-white hover:bg-red-700 rounded-xl
- Ghost: text-slate-500 hover:text-slate-900 hover:bg-slate-50 rounded-xl

INPUTS:
- border border-slate-200 rounded-xl bg-white px-4 py-2.5 text-sm
- focus:outline-none focus:ring-2 focus:ring-[#0C1E35]/20 focus:border-[#0C1E35]

REMOVE teal as primary everywhere except success states (toasts, checkmarks).
Primary action color = navy #0C1E35 only.

Apply consistently to:
- Dashboard.tsx
- SessionForm.tsx
- EndoscopyApp.tsx
- ReportGenerator.tsx
- Settings.tsx
- AdminDashboard.tsx
- Gallery.tsx

═══════════════════════════════════════
4. DASHBOARD — FINAL CLEANUP
═══════════════════════════════════════
- Remove duplicate "Mulai Sesi Baru" button from dashboard body
- Keep ONE CTA only in empty state when sessions.length === 0
- Stats: 3 cards (Total Sesi, Sesi Bulan Ini, Total Media)
- Page padding: px-8 py-8 consistent
- Welcome: "Selamat datang," + userProfile.name navy font-bold

═══════════════════════════════════════
5. SETTINGS PAGE — TAB UI
═══════════════════════════════════════
Tab bar:
- Container: bg-slate-100 rounded-2xl p-1 inline-flex gap-1 mb-6
- Active: bg-white text-slate-900 font-semibold shadow-sm rounded-xl px-4 py-2
- Inactive: text-slate-500 hover:text-slate-700 px-4 py-2 rounded-xl

Tabs: "Profil", "Keamanan", "Langganan", "Backup & Restore"
- Dokter Institusi: hide Langganan tab, show info card instead

PROFIL TAB:
- Avatar circle (initials, navy, w-16 h-16) centered at top
- 2-column grid for fields where possible
- Read-only fields: bg-slate-50 text-slate-500 cursor-not-allowed

KEAMANAN TAB:
- Clean password change form
- Show/hide toggles on all password fields

LANGGANAN TAB (Personal only):
- Current plan card + status chip + days remaining progress bar
- "Perpanjang Sekarang" → existing modal
- Billing history empty state

BACKUP TAB:
- Keep all existing functionality
- Apply consistent card styling only